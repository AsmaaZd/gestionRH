<form method="post" action="{{ path('recruteur_delete', {'id': recruteur.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recruteur.id) }}">
    <button class="btn btn-danger btn-xs"><i class="fas fa-trash-alt"></i></button>
</form>
