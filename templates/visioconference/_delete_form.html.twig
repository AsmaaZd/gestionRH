<form method="post" action="{{ path('visioconference_delete', {'id': visioconference.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visioconference.id) }}">
    <button class="btn">Delete</button>
</form>
