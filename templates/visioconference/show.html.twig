{% extends 'base.html.twig' %}

{% block title %}Visioconference{% endblock %}

{% block body %}
    <h1>Visioconference</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visioconference.id }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ visioconference.lien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('visioconference_index') }}">back to list</a>

    <a href="{{ path('visioconference_edit', {'id': visioconference.id}) }}">edit</a>

    {{ include('visioconference/_delete_form.html.twig') }}
{% endblock %}
