{% extends 'base.html.twig' %}

{% block title %}Competence{% endblock %}

{% block body %}
    <h1>Competence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Competence</th>
                <td>{{ competence.competence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('competence_index') }}">Retour à la liste</a>

    <a href="{{ path('competence_edit', {'id': competence.id}) }}">Modifier</a>

    {{ include('competence/_delete_form.html.twig') }}
{% endblock %}
