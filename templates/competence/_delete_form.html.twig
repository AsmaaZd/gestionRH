<form method="post" action="{{ path('competence_delete', {'id': competence.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer?');">
    
   
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competence.id) }}">
    <button class="btn btn-danger btn-xs"><i class="fas fa-trash-alt"></i></button>
</form>
