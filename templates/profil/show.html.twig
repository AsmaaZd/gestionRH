{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profil.id }}</td>
            </tr>
            <tr>
                <th>NbAnneesExp</th>
                <td>{{ profil.nbAnneesExp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profil_index') }}">Retour à la liste</a>

    <a href="{{ path('profil_edit', {'id': profil.id}) }}">Modifier</a>

    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}
