<form method="post" action="{{ path('entretien_delete', {'id': entretien.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entretien.id) }}">
    <button class="btn btn-danger btn-xs"><i class="fas fa-trash-alt"></i></button>
</form>
